apiVersion: v1
kind: Service
metadata:
  name: employee-php-service
spec:
  type: NodePort
  selector:
    app: employee-php
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30080
      protocol: TCP
